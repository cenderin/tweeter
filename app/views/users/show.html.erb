<div class="well">
		<%= form_for(@tweet) do |f| %>
		<%= f.text_field(:post) %>
		<%= f.submit "tweet" %>
		<% end %>
		</div>
	</div>
</div>
	<div class="span6 offset2">
		<div class="well">
			<% @tweets.each do |tweet| %><p>
				<div class="well">
				<%= tweet.post %>
				<%= current_user.name %>
				<%= tweet.created_at %>
				</div>
			<% end %>
			</p>
		</div>